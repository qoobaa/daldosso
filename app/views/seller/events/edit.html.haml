%h1 Editing event

= error_messages_for :event
- form_for :event, :url => url_for_order_event(@order, @event), :html => { :method => :put } do |f|
  = render :partial => 'form', :locals => { :f => f }
  = submit_tag 'Edit'
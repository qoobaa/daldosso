%h1 Editing product_group

= error_messages_for :product_group

- form_for :product_group, :url => admin_product_group_path(@product_group), :html => { :method => :put } do |f|
  = render :partial => 'form', :locals => { :f => f }
  = submit_tag 'Edit'
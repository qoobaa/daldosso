%h1 Creating new product group

= error_messages_for :product_group

- form_for :product_group, :url => admin_product_groups_path do |f|
  = render :partial => 'form', :locals => { :f => f }
  = submit_tag 'Create'

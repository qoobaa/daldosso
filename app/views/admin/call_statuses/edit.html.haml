%h1 Editing call status

= error_messages_for :call_status

- form_for :call_status, :url => admin_call_status_path(@call_status), :html => { :method => :put } do |f|
  = render :partial => 'form', :locals => { :f => f }
  = submit_tag 'Edit'
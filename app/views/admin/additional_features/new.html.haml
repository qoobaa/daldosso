%h1 Creating new additional feature

= error_messages_for :additional_feature

- form_for :additional_feature, :url => admin_additional_features_path do |f|
  = render :partial => 'form', :locals => { :f => f }
  = submit_tag 'Create'

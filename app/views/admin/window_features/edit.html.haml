%h1 Editing window feature

= error_messages_for :window_feature

- form_for :window_feature, :url => admin_window_feature_path(@window_feature), :html => { :method => :put } do |f|
  = render :partial => 'form', :locals => { :f => f }
  %p= submit_tag 'Edit'

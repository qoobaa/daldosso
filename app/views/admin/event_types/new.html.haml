%h1 Creating new event type

= error_messages_for :event_type

- form_for :event_type, :url => admin_event_types_path do |f|
  = render :partial => 'form', :locals => { :f => f }
  = submit_tag 'Create'

%h1 Creating new dependency

= error_messages_for :dependency

- form_for :dependency, :url => admin_dependencies_path do |f|
  = render :partial => 'form', :locals => { :f => f }
  = submit_tag 'Create'

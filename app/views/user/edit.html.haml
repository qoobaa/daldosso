%h1 Editing user profile

= error_messages_for :user

- form_for :user, :url => user_path, :html => { :method => :put } do |f|
  = render :partial => 'form', :locals => { :f => f }
  %p= submit_tag 'Edit'
